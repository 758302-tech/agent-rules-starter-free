@AGENTS.md

## Claude Code Notes

- Treat this file as project guidance, not an enforcement mechanism.
- Keep project instructions concise; if this file grows too large, split topic-specific guidance into `.claude/rules/`.
- When a rule applies only to certain paths, make it path-scoped instead of loading it into every session.
- If you repeatedly discover useful project facts, suggest adding them here.

